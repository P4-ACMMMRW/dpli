# DPL
## Build
How to build
```bash
chmod +x compile && ./compile
```

Then use
```bash
./dplc <file>
```
to run the dpl compiler

## Clean Build
To make a clean build run the following command:
```bash
./compile clean
```
