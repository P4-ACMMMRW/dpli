# DPL
## Build
### Linux
```bash
chmod +x compile.sh && ./compile.sh
```
Then use
```bash
cd build
./dplc <file>
```
to run the dpl compiler

#### Clean Build
To make a clean build run the following command:
```bash
./compile.sh clean
```
